name: Flight Alert Scraper

on:
  schedule:
    # 5:00 UTC → 7:00 CEST / 6:00 CET
    - cron: '0 5 * * *'
    # 14:00 UTC → 16:00 CEST / 15:00 CET
    - cron: '0 14 * * *'
    # 20:00 UTC → 22:00 CEST (estate) — notifica serale
    - cron: '0 20 * * *'
    # 21:00 UTC → 22:00 CET (inverno) — notifica serale
    - cron: '0 21 * * *'
  workflow_dispatch:
    inputs:
      departure_date:
        description: 'Data andata (YYYY-MM-DD) — lascia vuoto per ricerca generica'
        required: false
        type: string
      return_date:
        description: 'Data ritorno (YYYY-MM-DD) — lascia vuoto per solo andata'
        required: false
        type: string
      send_telegram:
        description: 'Invia notifica Telegram con i risultati'
        required: false
        default: true
        type: boolean
      commit_csv:
        description: 'Forza il commit di prices.csv (default: solo sera)'
        required: false
        default: false
        type: boolean

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ github.token }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: scraper/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: scraper

      - name: Run scraper
        run: npm start
        working-directory: scraper
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DEPARTURE_DATE: ${{ inputs.departure_date || '' }}
          RETURN_DATE: ${{ inputs.return_date || '' }}
          SEND_TELEGRAM: ${{ inputs.send_telegram || 'false' }}

      - name: Check if evening in Rome
        id: evening-check
        run: |
          HOUR=$(TZ='Europe/Rome' date +%H)
          if [ "$HOUR" -ge 18 ] && [ "$HOUR" -le 23 ]; then
            echo "is_evening=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_evening=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit updated prices
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "flight-alert-bot"
          git config user.email "bot@flight-alert.dev"
          git add data/prices.json
          if [ "${{ steps.evening-check.outputs.is_evening }}" = "true" ] || [ "${{ inputs.commit_csv || 'false' }}" = "true" ]; then
            test -f data/prices.csv && git add data/prices.csv || true
          fi
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "snapshot $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            git push "https://x-access-token:$(gh auth token)@github.com/${GITHUB_REPOSITORY}.git" HEAD:${GITHUB_REF_NAME}
          fi
